class ProfileAiEntity {
  final String advice;    // نصيحة الـ AI
  final String userName;  // اسم المستخدم (بدل ما نكتبه ثابت David)
  final String userImage; // رابط صورة المستخدم

  ProfileAiEntity({
    required this.advice,
    required this.userName,
    required this.userImage,
  });
}
name: prfilepage
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:

  dio: ^5.4.0  # تأكد من وجود هذا السطر
  http: ^1.1.0
  flutter:
    sdk: flutter

  # --- Firebase Ecosystem ---
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.1.0 # السطر ده مكانه الصح هنا

  # --- State Management & Logic ---
  flutter_bloc: ^8.1.6
  dartz: ^0.10.1
  get_it: ^7.7.0
  equatable: ^2.0.5

  # --- Navigation ---
  go_router: ^13.2.0

  # --- UI & Styling ---
  flutter_screenutil: ^5.9.3
  responsive_framework: ^1.5.1
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.3.0
  flutter_rating_bar: ^4.0.1
  modal_progress_hud_nsn: ^0.5.1
  pin_code_fields: ^8.0.1

  # --- Files & Assets ---
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

